{
  plugins.mini = {
    enable = true;
    modules = {
      ai = { };
      icons = { };
      notify = { };
      pairs = { };
      surround = { };
      tabline = { };
      trailspace = { };
    };
  };
}
